service: books

provider:
  name: aws
  runtime: python3.11
  timeout: 900

package:
  exclude:
    - .gitignore
    - .git/**
    - .vscode/**
    - __pycache__/**
    - node_modules/**
    - venv/**
    - package-lock.json
    - package.json

custom:
  serverless-offline:
    httpPort: 8888

functions:
  listBooks:
    handler: functions/books/list_books.handler
    events:
      - httpApi:
          path: /books
          method: GET
  
  getBook:
    handler: functions/books/get_book_by_id.handler
    events:
      - httpApi:
          path: /books/{bookId}
          method: GET
  
  createBook:
    handler: functions/books/create_book.handler
    events:
      - httpApi:
          path: /books
          method: POST
  
  updateBook:
    handler: functions/books/update_book.handler
    events:
      - httpApi:
          path: /books/{bookId}
          method: PUT
      - httpApi:
          path: /books/{bookId}
          method: PATCH
  
  deleteBook:
    handler: functions/books/delete_book.handler
    events:
      - httpApi:
          path: /books/{bookId}
          method: DELETE

plugins:
  - serverless-python-requirements
  - serverless-offline